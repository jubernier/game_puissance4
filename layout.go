package main

// Taille de l'affichage en fonction de la taille de la fenêtre.
func (g *Game) Layout(windowWidth, windowHeight int) (screenWidth, screenHeight int) {
	return globalWidth, globalHeight
}
